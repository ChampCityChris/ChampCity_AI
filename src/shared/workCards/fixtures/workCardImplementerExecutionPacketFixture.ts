import type { WorkCard } from "../workCardSchema";

export const workCardImplementerExecutionPacketFixture: WorkCard = {
  workCardId: "WC05",
  title: "Generate Implementer prompt",
  phase: "phase-01",
  status: "ready_for_implementer",
  createdAt: "2026-06-29T00:00:00.000Z",
  updatedAt: "2026-06-29T00:00:00.000Z",
  problem:
    "ChampCity A/I needs a manual Implementer prompt generation step that turns validated Work Card artifacts and optional supporting context into a precise copy-ready Implementer prompt without calling an LLM API or executing Implementer work.",
  goal:
    "Add an Implementer Execution Packet Generator screen and shared deterministic renderer that lets the Operator select a saved Work Card JSON artifact, optionally include supporting artifacts, copy the generated Implementer prompt, and save it as durable Markdown.",
  userOutcome:
    "The Operator can complete the manual MVP handoff loop by selecting a validated Work Card, reviewing a generated Implementer prompt, copying it for Implementer use, and saving the prompt artifact under the phase planning folder.",
  scope: [
    "Add minimal navigation for `Implementer Execution Packet Generator` alongside the existing Work Card screens.",
    "Allow selecting saved Work Card JSON files from `planning/phases/<phase-folder>/Work_Cards/` with `phase-01` as the default phase.",
    "Provide optional selectors for matching Work Card Markdown, Architect Prompt, Risk Review, and Prior Implementer Report artifacts.",
    "Default optional selectors to matching artifacts by Work Card ID where feasible and show plain-language notes when optional artifacts are missing.",
    "Add constrained main/preload IPC to list and read approved planning artifact folders and to save Implementer Execution Packet Markdown under `Implementer_Execution_Packets`.",
    "Validate loaded Work Card JSON with the existing Work Card validator before prompt generation.",
    "Create shared deterministic Implementer prompt rendering logic under `src/shared/workCards/`.",
    "Generate prompts from the Work Card JSON as the primary source of truth, with selected supporting artifacts included as context only.",
    "Add copy-to-clipboard behavior using browser clipboard APIs with a plain-language fallback error.",
    "Create the WC05 Work Card JSON and Markdown artifacts and the WC05 Implementer Report.",
    "Extend lightweight Work Card validation coverage for Implementer prompt rendering and path safety.",
  ],
  outOfScope: [
    "Do not call an LLM API.",
    "Do not implement automatic Architect reasoning.",
    "Do not execute Implementer work.",
    "Do not generate prompts for simple fixes or repairs yet.",
    "Do not modify selected Work Cards.",
    "Do not update Work Card status.",
    "Do not update Work Card `riskLevel`.",
    "Do not implement Work Card editing.",
    "Do not implement Implementer Report capture workflow.",
    "Do not implement validation or repair loops.",
    "Do not add OpenAI, Anthropic, Ollama, Featherless, LM Studio, or other provider integrations.",
    "Do not add database, cloud, auth, deployment, MCP, or connector integrations.",
    "Do not perform a broad UI redesign.",
    "Do not refactor unrelated source files.",
  ],
  requirements: [
    "Renderer code must not read or write files directly.",
    "Filesystem reads and writes must be mediated by constrained main/preload IPC.",
    "IPC must normalize and validate paths, reject traversal, reject arbitrary absolute paths, restrict phase folder names to safe values, and read only `.json` Work Cards and `.md` supporting artifacts.",
    "The Implementer prompt renderer must be deterministic string generation only and must not call an LLM, external API, filesystem API, or shell command.",
    "The generated Implementer prompt must include role and repository, Work Card identity, required repo checks, goal, problem, user outcome, scope, out of scope, requirements, acceptance criteria, validation plan, risk review summary, supporting artifact context, Implementer Report requirement, Git instructions, and out-of-scope guard language.",
    "Always include validation commands: `npm run typecheck`, `npm run build`, `npm test`, `npm run test:work-cards`, and `git status --short`.",
    "Include high-risk warning language when selected Risk Review context appears high risk.",
    "Include no-risk-review warning language when no Risk Review artifact is selected.",
    "Save Implementer Execution Packet artifacts with filenames matching `IMPLEMENTER_EXECUTION_PACKET_<work_card_id>_<slug>.md`.",
    "WC05 must not edit Work Card JSON or Markdown, change Work Card status, change Work Card `riskLevel`, push to GitHub, or create release tags.",
  ],
  acceptanceCriteria: [
    "Navigation shows `New Work Card`, `Architect Prompt Composer`, `Risk Router`, and `Implementer Execution Packet Generator`.",
    "Implementer Execution Packet Generator lists saved Work Card JSON files for `phase-01`.",
    "Selecting a Work Card displays its ID, title, phase, status, and risk level.",
    "Optional artifact selectors appear for Work Card Markdown, Architect Prompt, Risk Review, and Prior Implementer Report.",
    "Matching optional artifacts are selected by default where feasible.",
    "Missing optional artifacts show a plain-language note and do not block prompt generation.",
    "Generated Implementer prompt is visible and includes repo checks, validation commands, out-of-scope guard, Git instructions, and Implementer Report requirement.",
    "High-risk Risk Review context produces the required high-risk warning language.",
    "Missing Risk Review context produces a warning but does not block generation.",
    "`Copy Prompt` works or shows a plain-language fallback error.",
    "Saving creates Markdown under `planning/phases/phase-01/Implementer_Execution_Packets/`.",
    "Work Card JSON and Markdown files are not modified.",
    "`npm run typecheck`, `npm run build`, `npm test`, and `npm run test:work-cards` pass.",
  ],
  validationPlan: [
    "Run `npm run typecheck`.",
    "Run `npm run build`.",
    "Run `npm test`.",
    "Run `npm run test:work-cards`.",
    "Run `git status --short` and review changed files.",
    "Manually run `npm start` to validate the Electron UI.",
  ],
  riskLevel: "medium",
  risks: [
    "Manual Electron UI validation is still required because no renderer smoke test exists yet.",
    "Path safety must remain constrained as more planning artifact folders are listed and read.",
    "Prompt wording must preserve the Work Card as source of truth and must not imply approval without selected Risk Review context.",
    "Supporting artifacts could duplicate Work Card content, so selected context should be bounded without being omitted.",
  ],
  implementerInstructions: [
    "Verify the repository path before editing.",
    "Read `AGENTS.md`, the WC01-WC04 Implementer Reports, and the existing WC01-WC04 Work Card JSON artifacts before implementation.",
    "Keep the pass limited to WC05 Implementer prompt generation, constrained IPC, prompt saving, validation coverage, the WC05 Work Card artifacts, and the Implementer Report.",
    "Do not call LLM APIs, execute Implementer work, modify selected Work Cards, update Work Card statuses or risk levels, add provider SDKs, or implement unrelated features.",
    "Run the required validation commands and document results in the Implementer Report.",
    "Stage only files created or modified for WC05 and commit with `feat: add implementer execution packet generator`.",
  ],
  operatorNotes: [
    "A Work Card is the smallest buildable unit of work that can be handed to an Implementer.",
    "A Work Card should never be authored by the Operator alone.",
    "WC05 generates an Implementer prompt from validated artifacts only.",
    "The Implementer Execution Packet Generator must not perform Architect reasoning, revise the Work Card, or execute Implementer work.",
    "This completes the first manual MVP handoff loop: capture draft Work Card, generate Architect framing prompt, generate Risk Review, generate Implementer prompt.",
  ],
};
