# Main Storage

Future file-backed Markdown persistence belongs here.

Planned modules:

- `fileStore.ts`
- `artifactWriter.ts`

Writes must be constrained to approved planning paths and mediated through main/preload IPC.
