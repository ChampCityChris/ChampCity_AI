# Open Questions

1. What GitHub remote URL should this repo use if none is configured?
2. Should the GitHub repo be public or private if it must be created later?
3. What app name should appear in the Electron window and package metadata if different from ChampCity_AI?
4. Should a frontend framework such as React be added later, or should the MVP remain plain TypeScript/DOM until complexity requires one?
5. What validation commands should be required before each release tag?
6. Should evidence attachments be copied into the repo, linked by path, or both?
7. Which future LLM providers should be supported after the manual copy/paste MVP?
