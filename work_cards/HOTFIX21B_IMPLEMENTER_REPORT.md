# HOTFIX21B Implementer Report

- Card: Hotfix Work Card HOTFIX21B, sequence 2 of 6.
- Repository: verified the approved `<PROJECT_REPO>` Git top level before edits. Branch `hotfix/git-mcp-capability-completeness`, starting commit `24d940a`; configured origin retained, no remote mutation. The original dirty checkout remains preserved.
- Implementation: extended parameterless-compatible `diff` with unstaged/staged/between_refs views, optional contained paths, and exact comparison commits. Added structured `changed_files`, single-commit inspection with optional patch, ref comparison, tag inventory, safe remote inspection, selected-path unstaging, and explicit tracked-file restoration.
- Shared primitives: extracted the application's NUL-delimited changed-file parser into `gitMutations.ts`; both repository/MCP diff and application staged diff now call `inspectGitDiff`. SourceControlService exposes the new primitives through its existing receipt wrapper. No duplicate application implementation was introduced.
- Restore checks all selections before mutation, rejects wildcards/directories/symlinks/unmerged or untracked files, resolves the source once, uses literal pathspecs, and changes only the working tree. Ref and path precondition failures occur before the restore command. Filesystem failures or external concurrent mutation are not claimed to be transactional rollback.
- Inspection uses bounded Git output, explicit refs, NUL-delimited paths, and omits remote URLs. Commit metadata and optional patch redact local paths and credential-bearing URL user information. Merge-commit changes are measured against the first parent, with all parents returned.
- Modified: `src/main/agentHarness/repository/gitMutations.ts`, `src/main/agentHarness/repository/repositoryOperations.ts`, `src/main/agentHarness/tools/toolRegistry.ts`, `src/main/sourceControl/sourceControlService.ts`, `src/shared/sourceControlContracts.ts`, `test/agent-harness/reserved-toolbox-namespace.test.cjs`, and `test/agent-harness/agent-harness-core.test.cjs`.
- Created: `work_cards/HOTFIX21B_IMPLEMENTER_REPORT.md`. Deleted: none. Intentionally not created: dependencies, migrations, shell/raw Git access, worktree APIs, or new permanent test files.
- Tests: only the existing namespace and OAuth action inventories were maintained. Existing behavioral suites were retained; none were run, consolidated, or retired. No new behavioral proof is claimed. Capability-map ownership was inspected in A and remains unchanged because this pass adds no permanent test cases.
- Validation: `npm run build`, the three named `node --test --test-concurrency=1 test/agent-harness/{git-mutation-boundary,reserved-toolbox-namespace,agent-harness-core}.test.cjs` commands, `npm run typecheck`, and `npm test` are skipped at the Operator's explicit direction. No launch, packaging, or external integration validation was performed.
- Non-execution review: exact staged diff reviewed; `git diff --cached --check` and bounded staged secret/local-path/generated-artifact scan passed in the normal Windows lane. No dependency or lockfile change.
- Git actions: scoped stage and per-card commit authorized by the Operator. Commit hash pending at report write and supplied after commit; no push, merge, tag, rebase, release, stash, reset, or clean of the task repository.
- Residual risk: behavior and compilation are unverified in this pass because testing was explicitly skipped. Packet Architect review and Operator disposition remain. Next: read HOTFIX21C separately after this commit.
